# GoHighLevel — optional in Phase 1.
# The contact form still succeeds locally without these.
# Do not commit real tokens.

# Private Integration Token (Settings → Private Integrations).
# Needs Contacts write for the later GHL handoff.
GHL_PIT=

# Ajax Fitness location (free GHL account, not GLM).
GHL_LOCATION_ID=9s4QUTb46DnuhaD2NEca

# Optional inbound webhook as an alternative to the Contacts API.
GHL_WEBHOOK_URL=
